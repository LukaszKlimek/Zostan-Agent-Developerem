# 📈 Funkcje i Wykresy dla AI Engineering


---

## 🎯 Po co mi to w AI?

| Gdzie w AI? | Jaką funkcję tam zobaczysz? |
|-------------|----------------------------|
| Sieci neuronowe | Funkcje aktywacji (ReLU, sigmoid, tanh) |
| Trening modelu | Funkcja straty (loss function) |
| Klasyfikacja | Sigmoid zamieniający liczby na prawdopodobieństwa |
| Regresja liniowa | Prosta liniowa `y = ax + b` |
| Optymalizacja | Szukanie minimum funkcji straty |

**Funkcje = przepis na przekształcanie danych wejściowych w wyjściowe. To esencja każdego modelu ML!**

---

## 1️⃣ CZYM JEST FUNKCJA?

### Definicja

Funkcja to **reguła**, która każdej wartości wejściowej `x` przypisuje **dokładnie jedną** wartość wyjściową `y`.

```
         FUNKCJA
x ──────────────────→ y
(wejście)           (wyjście)
```

Zapisujemy: `y = f(x)` lub `f: x → y`

### Przykłady funkcji w AI

```python
# Regresja liniowa - prosta funkcja liniowa
def przewiduj_cene(powierzchnia):
    return 5000 * powierzchnia + 100000

# Sigmoid - zamienia dowolną liczbę na wartość 0..1
def sigmoid(x):
    return 1 / (1 + e^(-x))

# ReLU - jedna z najczęstszych funkcji aktywacji
def relu(x):
    return max(0, x)
```

### Co to jest dziedzina i zbiór wartości?

```
Dziedzina (domain) = zbiór DOZWOLONYCH wartości x
Zbiór wartości (range) = zbiór MOŻLIWYCH wartości y

Przykład: f(x) = √x
  Dziedzina: x ≥ 0  (nie możemy brać pierwiastka z ujemnej)
  Zbiór wartości: y ≥ 0  (pierwiastek zawsze nieujemny)
```

---

## 2️⃣ TYPY FUNKCJI WAŻNYCH W AI

### 📏 Funkcja liniowa

```
f(x) = ax + b

gdzie:
  a = nachylenie (slope) — jak strome jest wzniesienie
  b = wyraz wolny (intercept) — gdzie linia przecina oś Y
```

**Wykres:**
```
y
│          /
│         /  ← a = nachylenie (kąt prostej)
│        /
│   b → *    ← punkt przecięcia z osią Y
│      /
└──────────── x
```

**Przykład:** model przewiduje cenę domu
```
cena = 5000 × powierzchnia + 100000
        ↑ a                    ↑ b
   (5000 zł za m²)    (bazowa cena)
```

---

### 🔔 Funkcja kwadratowa

```
f(x) = ax² + bx + c
```

**Wykres (parabola):**
```
y
│    *
│   * *
│  *   *
│ *     *
└──────────── x
  minimum ↑
(gdy a > 0)
```

**W AI:** Funkcje straty (loss functions) często mają kształt paraboli — szukamy minimum!

---

### 🌊 Sigmoid — serce klasyfikatorów

```
           1
σ(x) = ─────────
        1 + e^(-x)
```

**Wykres:**
```
y
1.0 │         ──────────────
    │        /
0.5 │───────*───────────────  ← σ(0) = 0.5
    │      /
0.0 │─────────────────────
    └──────────────────────── x
     -5   -2   0   2    5
```

**Właściwości:**
- Zawsze zwraca wartość między **0 a 1** ✅
- Dla bardzo dużych x → 1 (prawie pewne)
- Dla bardzo małych x → 0 (prawie niemożliwe)
- Dla x = 0 → 0.5 (brak pewności)

**W AI:** Sigmoid zamienia wynik sieci neuronowej na **prawdopodobieństwo**.

```
Wynik sieci: 2.5  →  sigmoid(2.5) ≈ 0.92  →  "92% pewności klasy 1"
Wynik sieci: -1.0 →  sigmoid(-1.0) ≈ 0.27 →  "27% pewności klasy 1"
```

---

### ⚡ ReLU — najpopularniejsza funkcja aktywacji

```
ReLU(x) = max(0, x)
```

**Wykres:**
```
y
│            /
│           /
│          /
│─────────*            ← f(x) = 0 dla x < 0
└──────────────────── x
          0
```

**Prosto mówiąc:**
- Liczby ujemne → zamień na 0
- Liczby dodatnie → zostaw bez zmian

**Dlaczego tak popularna?**
```
Szybka do obliczenia: if x > 0 return x else return 0
Skuteczna w praktyce: eliminuje "nieważne" neurony
```

---

### 🌡️ Tanh — gdy potrzebujesz wartości -1 do 1

```
          e^x - e^(-x)
tanh(x) = ────────────
          e^x + e^(-x)
```

**Wykres:**
```
y
 1.0 │         ──────────────
     │        /
 0.0 │───────*───────────────  ← tanh(0) = 0
     │      /
-1.0 │─────────────────────
     └──────────────────────── x
```

Podobna do sigmoid, ale zakres to **-1 do 1** zamiast 0 do 1.

---

## 3️⃣ ZŁOŻENIE FUNKCJI

### Czym jest złożenie?

Złożenie to **funkcja wewnątrz funkcji** — wynik jednej staje się wejściem drugiej.

```
f(g(x))  →  najpierw g, potem f
```

**Przykład:**
```
g(x) = 2x + 1
f(x) = x²

f(g(x)) = (2x + 1)²

Dla x = 3:
  g(3) = 2×3 + 1 = 7
  f(7) = 7² = 49
```

### Złożenie w sieci neuronowej

Sieć neuronowa to **wielokrotne złożenie funkcji**:

```
Wejście → [warstwa 1] → [warstwa 2] → [warstwa 3] → Wyjście

x → f₁(w₁·x + b₁) → f₂(w₂·h₁ + b₂) → f₃(w₃·h₂ + b₃) → ŷ
```

Każda warstwa to: aktywacja(wagi · poprzednie_wyjście + bias)

---

## 4️⃣ ODCZYTYWANIE WYKRESÓW

### Co wykres mówi o funkcji?

```
                 PUNKT MAKSIMUM
                       ↓
y                      *
│                    *   *
│                  *       *
│                *           *
│──────────────*               *────
│            *                   *
└──────────────────────────────────── x
            ↑                    ↑
       wzrost funkcji        spadek funkcji
```

**Co warto odczytać:**
- **Minimum/maksimum** — gdzie funkcja osiąga ekstrema
- **Przejście przez zero** — gdzie `f(x) = 0`
- **Monotoniczność** — czy rośnie czy maleje
- **Zachowanie na krańcach** — co się dzieje dla dużych/małych x

### Przykład: wykres funkcji straty podczas trenowania

```
Loss
│\
│ \
│  \
│   ──\
│      ──\
│         ──────────────────
└──────────────────────────── Epoki treningowe

Dobry trening: loss spada i stabilizuje się
```

---

## 5️⃣ FUNKCJA WIELU ZMIENNYCH

W ML prawie zawsze mamy **wiele zmiennych wejściowych**:

```
f(x₁, x₂, ..., xₙ) = wynik

Przykład - przewidywanie ceny domu:
cena = f(powierzchnia, pokoje, dzielnica, rok_budowy)
     = 5000×powierzchnia + 30000×pokoje + 15000×dzielnica - 1000×wiek
```

**Wykres** takiej funkcji to powierzchnia w przestrzeni wielowymiarowej (trudna do wizualizacji, ale koncepcja ta sama).

---

## 📌 Podsumowanie — Kluczowe Funkcje w AI

| Funkcja | Wzór | Zastosowanie w AI |
|---------|------|-------------------|
| **Liniowa** | `y = ax + b` | Regresja liniowa, warstwy sieci |
| **Sigmoid** | `1/(1+e^(-x))` | Klasyfikacja binarna, wyjście modelu |
| **ReLU** | `max(0, x)` | Funkcja aktywacji w sieciach neuronowych |
| **Tanh** | `(eˣ-e⁻ˣ)/(eˣ+e⁻ˣ)` | Warstwy ukryte LSTM/RNN |
| **Softmax** | `eˣᵢ / Σeˣⱼ` | Klasyfikacja wieloklasowa (wyjście) |
| **MSE (strata)** | `(y - ŷ)²` | Funkcja straty w regresji |

---

## ✅ Sprawdź się

1. ✅ Jakie wartości zwraca funkcja sigmoid?
2. ✅ Co robi ReLU z liczbą ujemną?
3. ✅ Jak wygląda wykres funkcji liniowej z `a > 0`?
4. ✅ Dlaczego sigmoid nadaje się do klasyfikacji?

<details>
<summary>Odpowiedzi</summary>

1. Sigmoid zwraca wartości z przedziału **(0, 1)** — zawsze
2. ReLU zamienia liczbę ujemną na **0**, dodatnie zostawia bez zmian
3. Rosnąca linia prosta (im większe x, tym większe y)
4. Ponieważ zwraca wartości 0-1, które można interpretować jako **prawdopodobieństwo** przynależności do klasy

</details>

---

Rozumiejąc funkcje, możesz teraz zrozumieć jak modele ML **przekształcają dane** i dlaczego wybór odpowiedniej funkcji aktywacji ma znaczenie dla działania sieci.
