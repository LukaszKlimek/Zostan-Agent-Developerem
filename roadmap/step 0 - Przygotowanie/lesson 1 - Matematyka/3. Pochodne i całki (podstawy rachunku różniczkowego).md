# 📉 Pochodne i Całki (Podstawy Rachunku Różniczkowego)

---

# CZĘŚĆ I: MATEMATYKA

---

## 1️⃣ INTUICJA: CO MIERZY POCHODNA?

### Zacznijmy od prędkości

Wyobraź sobie, że jedziesz samochodem. Twoja pozycja na drodze zmienia się w czasie.

```
Czas (minuty):    0    1    2    3    4    5
Pozycja (km):     0    2    4    6    8    10
```

Pytanie: jak szybko jedziesz?

**Prędkość średnia** = zmiana pozycji ÷ zmiana czasu

```
Między minutą 0 a minutą 5:
  Prędkość średnia = (10 - 0) / (5 - 0) = 10 / 5 = 2 km/min
```

Ale co jeśli chcesz wiedzieć jak szybko jedziesz **dokładnie** w chwili t = 2 min?

To jest właśnie **prędkość chwilowa** — i do jej obliczenia potrzebna jest pochodna.

### Idea: "co by się stało, gdyby czas zmienił się o nieskończenie mały krok?"

Patrzysz na to, jak zmienia się pozycja gdy czas zmienia się o coraz mniejszy interwał:

```
Interwał czasu: 1 min   → prędkość średnia: (f(3) - f(2)) / 1 = (6 - 4) / 1 = 2
Interwał czasu: 0.5 min → prędkość średnia: (f(2.5) - f(2)) / 0.5 = ...
Interwał czasu: 0.1 min → ...coraz mniejszy...
Interwał czasu: 0.001 min → ...
...
Interwał czasu → 0    → TO JEST właśnie pochodna!
```

Pochodna = prędkość zmiany funkcji w konkretnym punkcie, gdy patrzymy na nieskończenie mały interwał.

### Inne przykłady z życia

Pochodna pojawia się wszędzie tam, gdzie mierzysz **tempo zmiany**:

```
Pozycja samochodu w czasie      → pochodna = prędkość
Prędkość samochodu w czasie     → pochodna = przyspieszenie
Temperatura w ciągu dnia        → pochodna = jak szybko robi się cieplej/zimniej
Saldo konta w czasie            → pochodna = jak szybko rośnie/maleje
Liczba użytkowników aplikacji   → pochodna = tempo wzrostu użytkowników
```

---

## 2️⃣ GEOMETRYCZNA INTERPRETACJA POCHODNEJ

### Nachylenie siecznej

Narysuj wykres dowolnej funkcji. Weź dwa punkty na tym wykresie i połącz je linią prostą. Ta linia to **sieczna**.

```
y
│          B ← drugi punkt
│         /|
│        / |
│       /  |
│      /   | ← zmiana y (Δy)
│     /    |
│    A     |
│    |─────| ← zmiana x (Δx)
└────────────── x
```

**Nachylenie siecznej** = zmiana y ÷ zmiana x = Δy / Δx

### Nachylenie stycznej = pochodna

Teraz wyobraź sobie, że przesuwasz drugi punkt B coraz bliżej do punktu A. Sieczna obraca się i w końcu "przykleja się" do wykresu w punkcie A. Ta linia, która dotyka wykresu w jednym punkcie, to **styczna**.

```
y
│              /← styczna w punkcie A
│           __/
│        __/
│     __/ ← funkcja f(x)
│    /A
│   /
└──────────────── x

Nachylenie stycznej w punkcie A = f'(A) = pochodna w punkcie A
```

**Pochodna w danym punkcie = nachylenie stycznej do wykresu w tym punkcie.**

Jeśli wykres jest stromy → pochodna jest duża.
Jeśli wykres jest prawie poziomy → pochodna jest bliska zeru.
Jeśli wykres opada w dół → pochodna jest ujemna.

---

## 3️⃣ FORMALNA DEFINICJA POCHODNEJ

Matematycznie, pochodna funkcji `f` w punkcie `x` to:

```
         f(x + h) - f(x)
f'(x) = lim ─────────────
        h→0       h
```

Co to znaczy krok po kroku:
- `f(x + h)` — wartość funkcji w punkcie przesuniętym o h
- `f(x + h) - f(x)` — o ile zmieniła się wartość funkcji
- Dzielimy przez h — to daje nam "nachylenie siecznej"
- `lim h→0` — patrzymy co się dzieje gdy h zbliża się do zera

Wynik oznaczymy: `f'(x)` lub `df/dx` (czytaj "df po dx")

### Przykład obliczenia z definicji

Obliczmy pochodną `f(x) = x²` z definicji:

```
f(x + h) = (x + h)² = x² + 2xh + h²

f(x + h) - f(x) = x² + 2xh + h² - x² = 2xh + h²

[f(x + h) - f(x)] / h = (2xh + h²) / h = 2x + h

Teraz "h → 0":
  lim (2x + h) = 2x + 0 = 2x
  h→0

Wynik: f'(x) = 2x
```

Na szczęście nie musimy zawsze liczyć z definicji. Są gotowe reguły.

---

## 4️⃣ REGUŁY RÓŻNICZKOWANIA

### Reguła 1: Pochodna stałej

```
f(x) = c  →  f'(x) = 0

Przykłady:
  f(x) = 7   →  f'(x) = 0
  f(x) = -3  →  f'(x) = 0
  f(x) = π   →  f'(x) = 0
```

**Dlaczego?** Stała się nie zmienia. Tempo zmiany czegoś niezmiennego = 0.

---

### Reguła 2: Reguła potęgi (Power Rule)

```
f(x) = xⁿ  →  f'(x) = n · xⁿ⁻¹

Mechanizm: "zepchnij wykładnik na dół i zmniejsz go o 1"
```

Przykłady:

```
f(x) = x¹   →  f'(x) = 1 · x⁰ = 1 · 1 = 1
f(x) = x²   →  f'(x) = 2 · x¹ = 2x
f(x) = x³   →  f'(x) = 3 · x²
f(x) = x⁴   →  f'(x) = 4 · x³
f(x) = x⁵   →  f'(x) = 5 · x⁴

f(x) = x^(-1) = 1/x   →  f'(x) = -1 · x^(-2) = -1/x²
f(x) = x^(1/2) = √x   →  f'(x) = (1/2) · x^(-1/2) = 1/(2√x)
```

---

### Reguła 3: Pochodna sumy/różnicy

```
(f + g)'(x) = f'(x) + g'(x)
(f - g)'(x) = f'(x) - g'(x)
```

Prosto: liczymy pochodne osobno i sumujemy.

```
Przykład: h(x) = x³ + 5x² - 2x + 7

h'(x) = (x³)' + (5x²)' - (2x)' + (7)'
       = 3x²  + 10x    -  2   +  0
       = 3x² + 10x - 2
```

---

### Reguła 4: Pochodna iloczynu przez stałą

```
(c · f)'(x) = c · f'(x)

Stała "wychodzi" przed pochodną.
```

```
Przykład:
  f(x) = 5x³
  f'(x) = 5 · (x³)' = 5 · 3x² = 15x²
```

---

### Reguła 5: Reguła łańcuchowa — KLUCZOWA!

Gdy mamy złożenie funkcji `f(g(x))`, pochodna wynosi:

```
[f(g(x))]' = f'(g(x)) · g'(x)

Słownie: "pochodna zewnętrzna (liczona dla wewnętrznej) × pochodna wewnętrznej"
```

To brzmi skomplikowanie, ale po przykładach staje się jasne.

**Przykład 1:**

```
h(x) = (x + 3)²

Zewnętrzna: f(u) = u²   →  f'(u) = 2u
Wewnętrzna: g(x) = x+3  →  g'(x) = 1

h'(x) = f'(g(x)) · g'(x)
       = 2(x + 3)  ·  1
       = 2(x + 3)
       = 2x + 6
```

**Przykład 2:**

```
h(x) = (3x² + 1)⁵

Zewnętrzna: f(u) = u⁵      →  f'(u) = 5u⁴
Wewnętrzna: g(x) = 3x² + 1 →  g'(x) = 6x

h'(x) = f'(g(x)) · g'(x)
       = 5(3x² + 1)⁴  ·  6x
       = 30x · (3x² + 1)⁴
```

**Przykład 3 — trzy zagnieżdżenia:**

```
h(x) = √(x² + 1)  =  (x² + 1)^(1/2)

Zewnętrzna: f(u) = u^(1/2)  →  f'(u) = (1/2) · u^(-1/2) = 1/(2√u)
Wewnętrzna: g(x) = x² + 1   →  g'(x) = 2x

h'(x) = 1/(2√(x² + 1))  ·  2x
       = x / √(x² + 1)
```

---

## 5️⃣ GDZIE JEST MINIMUM I MAKSIMUM?

### Kluczowa obserwacja

Przypomnij sobie co mówi pochodna geometrycznie: to nachylenie stycznej.

Przy minimum lub maksimum funkcja "wyrównuje" — styczna jest **pozioma**. Pozioma linia ma nachylenie **zero**.

```
y          *  ← maksimum, tutaj f'(x) = 0
│        *   *
│      *       *
│    *           *
│*                 *

y
│     *                  *
│    * *                * *
│   *   *              *   *
│  *     *    *       *
│          * * *  ← minimum, tutaj f'(x) = 0
```

**Zasada:** W punktach minimum i maksimum: `f'(x) = 0`

### Jak znaleźć minimum/maksimum?

Krok 1: Oblicz pochodną f'(x)
Krok 2: Ustaw f'(x) = 0 i znajdź x
Krok 3: Sprawdź czy to minimum czy maksimum (druga pochodna lub test)

```
Przykład: f(x) = x² - 4x + 5

Krok 1: f'(x) = 2x - 4

Krok 2: 2x - 4 = 0
        2x = 4
        x = 2

Krok 3: f(2) = 4 - 8 + 5 = 1  ← to jest wartość minimum

Sprawdzenie: f''(x) = 2 > 0  → parabola "uśmiechnięta" → minimum ✅
```

---

## 6️⃣ POCHODNE CZĄSTKOWE — DLA FUNKCJI WIELU ZMIENNYCH

Gdy funkcja ma wiele zmiennych, możemy obliczać pochodną **po każdej zmiennej osobno**, traktując pozostałe jak stałe.

### Notacja

```
∂f/∂x   — pochodna cząstkowa po x  (czytaj: "d f po d x")
∂f/∂y   — pochodna cząstkowa po y
```

### Przykład

```
f(x, y) = x² + 3xy + y²

Pochodna cząstkowa po x (y traktujemy jak stałą):
  ∂f/∂x = 2x + 3y

  (pochodna z x² = 2x, pochodna z 3xy względem x = 3y, pochodna z y² = 0)

Pochodna cząstkowa po y (x traktujemy jak stałą):
  ∂f/∂y = 3x + 2y

  (pochodna z x² = 0, pochodna z 3xy względem y = 3x, pochodna z y² = 2y)
```

**Sprawdzenie dla konkretnych wartości:**

```
Oblicz ∂f/∂x w punkcie (2, 3):
  ∂f/∂x = 2x + 3y = 2(2) + 3(3) = 4 + 9 = 13

Oznacza: gdy jesteśmy w punkcie (2,3), funkcja rośnie 13 razy szybciej
         w kierunku x niż x rośnie.
```

---

## 7️⃣ GRADIENT

### Definicja

**Gradient** to wektor złożony ze wszystkich pochodnych cząstkowych. Zapisujemy go symbolem ∇f (czytaj "nabla f").

```
Dla f(x, y):
  ∇f = [∂f/∂x, ∂f/∂y]

Dla f(x, y, z):
  ∇f = [∂f/∂x, ∂f/∂y, ∂f/∂z]
```

### Co gradient mówi geometrycznie?

Wyobraź sobie, że jesteś w górach i patrzysz na mapę warstwicową. Gradient wskazuje kierunek najstromszego wejścia pod górę z twojego aktualnego miejsca.

```
         ↗↑↖   ← gradient wskazuje kierunek najszybszego WZROSTU
        ↗  ↑  ↖
       ↗   ↑   ↖
          szczyt

    Im dalej od szczytu, tym dłuższe strzałki (większy gradient).
    Na samym szczycie: gradient = [0, 0]
```

**Kluczowa właściwość:**
- Gradient wskazuje kierunek **najszybszego wzrostu** funkcji
- **Minus gradientu** (-∇f) wskazuje kierunek **najszybszego spadku**

---

## 8️⃣ CAŁKI — OPERACJA ODWROTNA DO RÓŻNICZKOWANIA

### Intuicja: pole pod wykresem

Całka ma dwa znaczenia, które warto rozumieć:

**Znaczenie 1: Całka jako odwrotność pochodnej**

Jeśli pochodna to "różniczkowanie", całka to "całkowanie" — operacja odwrotna.

```
Pochodna z x² = 2x

Odwrotnie: całka z 2x = x²  (z dokładnością do stałej)
```

**Znaczenie 2: Całka oznaczona jako pole pod wykresem**

Całka od a do b funkcji f(x) to **pole obszaru** między wykresem a osią x.

```
y
│        *
│      * |█|
│    * |█|█|
│   *|█|█|█|
│  *|█|█|█|█|
└──────────────── x
   a           b

Zacieniowany obszar = ∫ᵃᵇ f(x) dx
```

### Podstawowy wzór całkowania (odwrotność reguły potęgi)

```
∫ xⁿ dx = xⁿ⁺¹ / (n+1) + C        (dla n ≠ -1)

Mechanizm: "zwiększ wykładnik o 1 i podziel przez nowy wykładnik"
```

Przykłady:

```
∫ 1 dx   = x + C          (bo pochodna z x = 1)
∫ x dx   = x²/2 + C       (bo pochodna z x²/2 = x)
∫ x² dx  = x³/3 + C       (bo pochodna z x³/3 = x²)
∫ x³ dx  = x⁴/4 + C       (bo pochodna z x⁴/4 = x³)
∫ 2x dx  = x² + C         (bo pochodna z x² = 2x)
∫ 3x² dx = x³ + C         (bo pochodna z x³ = 3x²)
```

Stała `+ C` pojawia się, bo pochodna stałej = 0, więc nie wiemy jaka stała "zniknęła" przy różniczkowaniu.

### Całka oznaczona — obliczanie pola

```
∫ᵃᵇ f(x) dx = F(b) - F(a)

gdzie F(x) to funkcja, której pochodna to f(x)  (tzw. funkcja pierwotna)
```

Przykład — oblicz pole pod wykresem `f(x) = x²` od x=1 do x=3:

```
Funkcja pierwotna: F(x) = x³/3

∫₁³ x² dx = F(3) - F(1)
           = 3³/3 - 1³/3
           = 27/3 - 1/3
           = 9 - 1/3
           = 8⅔  ≈ 8.67
```

### Weryfikacja: czy F' = f?

```
F(x) = x³/3   →   F'(x) = 3x²/3 = x²  ✅
```

---

## ✅ Sprawdź się — Część Matematyczna

1. Oblicz f'(x) dla `f(x) = 4x³ - 6x² + 2x - 1`
2. Oblicz pochodną `h(x) = (x² + 1)³` używając reguły łańcuchowej
3. Znajdź minimum funkcji `f(x) = x² - 6x + 10`
4. Oblicz `∫ (3x² + 2x) dx`

<details>
<summary>Odpowiedzi</summary>

1. `f'(x) = 12x² - 12x + 2`
2. Zewnętrzna: `(u³)' = 3u²`, wewnętrzna: `(x²+1)' = 2x`, wynik: `h'(x) = 3(x²+1)² · 2x = 6x(x²+1)²`
3. `f'(x) = 2x - 6 = 0` → `x = 3`, minimum: `f(3) = 9 - 18 + 10 = 1`
4. `x³ + x² + C`

</details>

---

---

# CZĘŚĆ II: JAK TO DZIAŁA W AI?

> Ta sekcja to tylko zajawka — zarys tego, dokąd zmierza ta matematyka. Nie musisz teraz rozumieć szczegółów. Ważne żebyś widział, że to wszystko ma sens.

---

### Co to jest "uczenie się" programu komputerowego?

Zanim przejdziemy do matematyki — wyjaśnijmy co to w ogóle znaczy, że program "się uczy".

Wyobraź sobie, że chcesz nauczyć program rozpoznawać, czy zdjęcie przedstawia kota czy psa. Na początku program nie wie nic. W środku ma pewne liczby (nazwijmy je "parametrami" lub "wagami") ustawione losowo. Na podstawie tych liczb program daje odpowiedź.

Oczywiście na początku odpowiedzi są złe — bo liczby są losowe.

**Uczenie** polega na tym, że stopniowo poprawiamy te liczby, żeby program dawał coraz lepsze odpowiedzi.

```
Losowe parametry → złe odpowiedzi
Poprawione parametry → trochę lepsze odpowiedzi
Bardziej poprawione parametry → jeszcze lepsze odpowiedzi
... (tysiące/miliony powtórzeń) ...
Dobrze dobrane parametry → dobre odpowiedzi
```

Ale jak "poprawić" parametry? Tu wkraczają pochodne.

### Funkcja straty — jak bardzo się mylimy?

Żeby mierzyć "jak złe" są odpowiedzi programu, używamy **funkcji straty** (ang. *loss function*). To liczba:
- Gdy program odpowiada dobrze → mała wartość (bliska 0)
- Gdy program odpowiada źle → duża wartość

Przykład bardzo prostej funkcji straty:

```
Prawdziwa odpowiedź: y = 1 (to jest kot)
Odpowiedź programu: ŷ = 0.3 (program mówi "30% szansy że to kot")

Strata = (y - ŷ)² = (1 - 0.3)² = 0.49
```

Chcemy minimalizować tę stratę — czyli szukamy minimum funkcji.

### Gradient descent — minimalizacja przez pochodne

Tu pojawia się gradient, który właśnie poznałeś.

Funkcja straty zależy od parametrów modelu (tych liczb w środku). Możemy obliczyć gradient tej funkcji — czyli dowiedzieć się, w którym kierunku należy zmienić parametry, żeby strata **rosła**.

Skoro wiemy w którym kierunku strata rośnie, to idąc w **przeciwnym kierunku** — strata będzie **maleć**.

```
Wyobraź sobie, że stoisz na zboczu góry i chcesz zejść do doliny.

Patrzysz na teren i wybierasz kierunek, który idzie najbardziej stromo w dół.
Robisz krok.
Znów patrzysz.
Znów krok.
...
Aż dojdziesz do doliny (minimum).
```

To jest dokładnie **gradient descent** — algorytm, który setki tysięcy razy oblicza gradient i przesuwa parametry modelu w kierunku malejącej straty.

```
Wzór aktualizacji parametrów:
  parametry_nowe = parametry_stare - (mały krok) × gradient_straty

Gradient mówi: "w tę stronę rośnie strata"
Minus mówi: "idź w PRZECIWNĄ stronę"
Mały krok → żeby nie przeskoczyć przez minimum
```

Nie musisz teraz wiedzieć jak to jest zaimplementowane. Ważne, że rozumiesz:
- **Pochodna/gradient = informacja o tym, w którą stronę zmienia się funkcja**
- **Minimalizacja = szukanie punktu gdzie pochodna = 0**
- **Program "uczy się" = stopniowo przesuwa parametry w kierunku mniejszej straty**

Reguła łańcuchowa, którą poznałeś, jest używana do obliczania tych gradientów przez całą strukturę modelu — ale to temat na dużo późniejszy etap kursu.

---

## 📌 Podsumowanie

| Pojęcie | Co to? |
|---------|--------|
| **Pochodna f'(x)** | Tempo zmiany funkcji; geometrycznie: nachylenie stycznej |
| **Reguła potęgi** | `(xⁿ)' = n·xⁿ⁻¹` |
| **Reguła łańcuchowa** | `[f(g(x))]' = f'(g(x)) · g'(x)` — dla złożeń |
| **Minimum/maksimum** | Tam gdzie `f'(x) = 0` |
| **Pochodna cząstkowa** | Pochodna po jednej zmiennej, pozostałe traktowane jak stałe |
| **Gradient ∇f** | Wektor wszystkich pochodnych cząstkowych |
| **Całka** | Operacja odwrotna do różniczkowania; geometrycznie: pole pod wykresem |
