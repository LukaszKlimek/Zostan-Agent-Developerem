# 📈 Funkcje i Wykresy

---

# CZĘŚĆ I: MATEMATYKA

---

## 1️⃣ CZYM JEST FUNKCJA?

### Intuicja z życia codziennego

Wyobraź sobie automat z kawą. Wciskasz przycisk "espresso" i dostajesz espresso. Wciskasz "latte" i dostajesz latte. Jeden przycisk = jeden określony wynik. Nigdy nie zdarza się, że wciskasz "espresso" i raz wychodzi espresso, a raz zupa.

To jest dokładnie to, czym jest funkcja w matematyce.

**Funkcja** to reguła, która każdej wartości wejściowej przypisuje **dokładnie jedną** wartość wyjściową.

```
         FUNKCJA
x ──────────────────→ y
(wejście)           (wyjście)

Przykłady z życia:
  wiek osoby   → data urodzenia          (każdy wiek ma jedną datę)
  numer buta   → długość stopy w cm      (rozmiar 42 = ok. 26.7 cm)
  dzień roku   → temperatura w danym dniu
```

Zapisujemy to jako: `y = f(x)`, co czytamy "y jest funkcją x" lub "f z x".

Litera `f` to nazwa funkcji (możemy używać dowolnej litery: g, h, k...).

### Co NIE jest funkcją?

Jeśli jedna wartość wejściowa daje WIELE różnych wyników — to nie jest funkcja.

```
Przykład: "kwadratowy pierwiastek z 9"

√9 = 3 i √9 = -3    ← dwa wyniki dla jednego wejścia!

Dlatego zazwyczaj definiujemy √x jako TYLKO wynik dodatni.
```

### Jak zapisujemy funkcje?

```
f(x) = 2x + 1        ← wzór algebraiczny

f(3) = 2 × 3 + 1 = 7
f(5) = 2 × 5 + 1 = 11
f(0) = 2 × 0 + 1 = 1
```

`f(3)` czytamy "f z 3" — to znaczy: "oblicz wartość funkcji f dla x = 3".

---

## 2️⃣ DZIEDZINA I ZBIÓR WARTOŚCI

### Dziedzina — gdzie funkcja "ma prawo" działać

**Dziedzina** (z angielskiego: *domain*) to zbiór wszystkich wartości `x`, dla których funkcja jest określona — czyli wszystkich dopuszczalnych wejść.

Myśl o tym jak o instrukcji do automatu: "ten automat przyjmuje tylko monety 1, 2 i 5 zł".

```
Przykład 1: f(x) = 2x + 1
  Dziedzina: wszystkie liczby rzeczywiste  (-∞, +∞)
  Nie ma żadnych ograniczeń — każdą liczbę można pomnożyć przez 2 i dodać 1.

Przykład 2: f(x) = √x
  Dziedzina: x ≥ 0  (czyli [0, +∞))
  Dlaczego? Bo nie możemy brać pierwiastka z liczby ujemnej (w liczbach rzeczywistych).
  √(-4) = ?  ← brak odpowiedzi w liczbach rzeczywistych

Przykład 3: f(x) = 1/x
  Dziedzina: x ≠ 0  (wszystkie liczby OPRÓCZ zera)
  Dlaczego? Bo dzielenie przez zero jest niemożliwe.
  1/0 = ?  ← "nieskończoność", czyli wynik jest niezdefiniowany
```

### Zbiór wartości — co funkcja może "wyprodukować"

**Zbiór wartości** (*range*) to wszystkie możliwe wartości wyjściowe `y`, które funkcja może zwrócić.

```
Przykład: f(x) = x²
  Dziedzina: wszystkie liczby rzeczywiste
  Zbiór wartości: y ≥ 0  (bo x² zawsze jest nieujemne)

  f(3) = 9  → wynik dodatni ✅
  f(-3) = 9 → wynik dodatni ✅
  f(0) = 0  → zero ✅
  Ale f(x) = -4 NIGDY nie da żadne x!  ← -4 nie jest w zbiorze wartości
```

---

## 3️⃣ TYPY FUNKCJI

### Funkcja stała

```
f(x) = c     (gdzie c to jakaś liczba, np. f(x) = 5)
```

Bez względu na to, co podasz na wejściu, zawsze dostaniesz to samo.

```
f(1) = 5
f(100) = 5
f(-50) = 5

Wykres:

y
│
5 │─────────────────────────  ← pozioma linia
│
└──────────────────────────── x
```

---

### Funkcja liniowa

```
f(x) = ax + b

gdzie:
  a = współczynnik kierunkowy (nachylenie linii)
  b = wyraz wolny (gdzie linia przecina oś Y, gdy x = 0)
```

**Nachylenie `a` mówi: "ile y rośnie, gdy x rośnie o 1".**

```
Przykłady:

f(x) = 2x + 1   →   a = 2, b = 1

  x = 0: f(0) = 2×0 + 1 = 1
  x = 1: f(1) = 2×1 + 1 = 3
  x = 2: f(2) = 2×2 + 1 = 5
  x = 3: f(3) = 2×3 + 1 = 7

  Za każdym razem gdy x rośnie o 1, y rośnie o 2 (bo a = 2).
```

**Wykres:**
```
y
│              * (3, 7)
│           *
│        * (2, 5)   ← linia prosta
│     *
│  * (1, 3)
│*(0, 1)
└──────────────── x
0  1  2  3
```

**Różne nachylenia:**
```
a > 0  → linia rośnie (idzie w górę z lewej do prawej)
a < 0  → linia maleje (idzie w dół z lewej do prawej)
a = 0  → linia pozioma (funkcja stała)

im większe |a|, tym bardziej "stroma" linia.
```

```
y                y                y
│     /          │\               │──────────
│    /    a=2    │  \  a=-1       │          a=0
│   /            │   \
│  /             │    \
└────────────    └───────────     └──────────
```

---

### Funkcja kwadratowa

```
f(x) = ax² + bx + c
```

Wykres tej funkcji to **parabola** — krzywa w kształcie litery U (lub ∩).

```
Prosty przykład: f(x) = x²

  x = -3: f(-3) = 9
  x = -2: f(-2) = 4
  x = -1: f(-1) = 1
  x =  0: f(0) = 0   ← wierzchołek paraboli (minimum)
  x =  1: f(1) = 1
  x =  2: f(2) = 4
  x =  3: f(3) = 9
```

**Wykres:**
```
y
9│*             *
 │
4│  *         *
 │
1│    *     *
 │
0│      *           ← minimum w (0, 0)
 └──────────────── x
  -3 -2 -1  0  1  2  3
```

**Ważna właściwość:**

- Gdy `a > 0` → parabola otwarta w górę, ma **minimum**
- Gdy `a < 0` → parabola otwarta w dół, ma **maksimum**

```
a > 0:            a < 0:
     U                ∩
 (minimum)         (maksimum)
```

---

### Wartość bezwzględna

```
|x| = x   gdy x ≥ 0
|x| = -x  gdy x < 0
```

Prosto mówiąc: zawsze zwraca liczbę nieujemną. To "odległość od zera".

```
|5| = 5
|-5| = 5
|0| = 0
|-3.7| = 3.7
```

**Wykres:** kształt litery V

```
y
│\             /
│ \           /
│  \         /
│   \       /
│    \     /
│     \   /
│      \ /
│       *           ← wierzchołek w (0, 0)
└──────────────── x
```

---

### Pierwiastek kwadratowy

```
f(x) = √x

Dziedzina: x ≥ 0
```

```
  x = 0: √0 = 0
  x = 1: √1 = 1
  x = 4: √4 = 2
  x = 9: √9 = 3
  x = 16: √16 = 4
```

**Wykres:** rośnie, ale coraz wolniej

```
y
│
4│               *  (16, 4)
│
3│          * (9, 3)
│
2│      * (4, 2)
│
1│  * (1, 1)
│
0│* (0, 0)
└──────────────────── x
0  1  4  9  16
```

---

## 4️⃣ JAK CZYTAĆ WYKRES FUNKCJI?

Wykres to graficzna reprezentacja funkcji. Każdy punkt na wykresie to para (x, y) gdzie y = f(x).

### Co warto umieć odczytać z wykresu?

**Czy funkcja rośnie czy maleje?**

Patrząc od lewej do prawej:
- Linia idzie w górę → funkcja **rośnie**
- Linia idzie w dół → funkcja **maleje**

```
y
│         /\
│        /  \
│       /    \       ← tu maleje
│      /      \
│     /        \
│    /  tu rośnie
└────────────────── x
```

**Minimum i maksimum:**

- **Minimum** — najniższy punkt na wykresie (lub lokalnie najniższy)
- **Maksimum** — najwyższy punkt

```
y                           y
│          *               │*             *
│        *   *             │  *         *
│      *       *           │    *     *
│    *                     │      * ←── minimum lokalne
└───────────────── x       └───────────────── x
        ↑                           ↑
    maksimum                    minimum
```

**Gdzie funkcja = 0? (miejsca zerowe)**

To punkty, w których wykres **przecina oś x**.

```
y
│    *
│   * *
│  *   *
│ *     *
│*       *
──────────────── x  ← f(x₁) = 0 i f(x₂) = 0
       ↑ ↑
     x₁  x₂
```

**Jak wygląda funkcja dla bardzo dużych i bardzo małych x?**

```
f(x) = x²:
  x → +∞:   y → +∞  (rośnie bez ograniczeń)
  x → -∞:   y → +∞  (też rośnie, bo (-100)² = 10000)

f(x) = 1/x:
  x → +∞:   y → 0   (zbliża się do zera, ale nigdy go nie osiąga)
  x → -∞:   y → 0
```

---

## 5️⃣ ZŁOŻENIE FUNKCJI

### Co to jest?

Złożenie funkcji to sytuacja, gdy **wynik jednej funkcji staje się wejściem drugiej**.

```
wejście x → [funkcja g] → wynik pośredni → [funkcja f] → wynik końcowy

Zapisujemy: f(g(x))  lub  (f ∘ g)(x)
Czytamy: "f złożone z g" lub "f z g z x"
```

### Przykład krok po kroku

```
Mamy:
  g(x) = 2x + 1
  f(x) = x²

Oblicz f(g(3)):

Krok 1: Najpierw oblicz g(3)
  g(3) = 2 × 3 + 1 = 7

Krok 2: Wynik g(3) wstaw do f
  f(7) = 7² = 49

Odpowiedź: f(g(3)) = 49
```

### Ogólny wzór

```
f(g(x)) = f(2x + 1) = (2x + 1)²

Sprawdzenie dla x = 3:
  (2 × 3 + 1)² = 7² = 49  ✅
```

### Kolejność ma znaczenie!

```
f(g(x)) ≠ g(f(x))   (w ogólności!)

Przykład:
  f(x) = x + 1
  g(x) = x²

  f(g(x)) = x² + 1      ← najpierw podnosimy do kwadratu, potem dodajemy 1
  g(f(x)) = (x + 1)²    ← najpierw dodajemy 1, potem podnosimy do kwadratu

  Dla x = 3:
    f(g(3)) = 9 + 1 = 10
    g(f(3)) = (3 + 1)² = 16

  10 ≠ 16  ✅ Różne wyniki!
```

---

## 6️⃣ FUNKCJE WIELU ZMIENNYCH

Do tej pory mieliśmy funkcje jednej zmiennej: `f(x)`. Ale funkcja może przyjmować wiele wartości wejściowych jednocześnie.

```
f(x, y) = x + y          ← dwie zmienne
f(x, y, z) = x² + y² + z²  ← trzy zmienne
```

### Przykład z życia

Obszar prostokąta zależy od dwóch zmiennych:
```
Pole(długość, szerokość) = długość × szerokość

Pole(3, 4) = 3 × 4 = 12
Pole(5, 2) = 5 × 2 = 10
```

### Wykres funkcji dwóch zmiennych

Funkcja dwóch zmiennych tworzy **powierzchnię** w przestrzeni 3D, nie linię.

```
Wyobraź sobie mapę wysokości terenu:
  - wejście: szerokość geograficzna x, długość geograficzna y
  - wyjście: wysokość nad poziomem morza

Każde miejsce na mapie (x, y) odpowiada jednej wysokości f(x, y).
```

---

## ✅ Sprawdź się — Część Matematyczna

1. Oblicz `f(4)` dla `f(x) = 3x - 2`
2. Jaką dziedziną ma funkcja `f(x) = 1/(x-3)`?
3. Dla `g(x) = x + 2` i `f(x) = x²`, ile wynosi `f(g(1))`?
4. Dlaczego `√(-9)` nie jest liczbą rzeczywistą?

<details>
<summary>Odpowiedzi</summary>

1. `f(4) = 3×4 - 2 = 12 - 2 = 10`
2. Wszystkie liczby oprócz 3, bo dla x=3 mielibyśmy 1/0 (dzielenie przez zero)
3. `g(1) = 1 + 2 = 3`, następnie `f(3) = 3² = 9`
4. Bo nie istnieje liczba rzeczywista, której kwadrat jest ujemny

</details>

---

---

# CZĘŚĆ II: JAK TO DZIAŁA W AI?

> Ta sekcja to tylko zajawka. Nie musisz teraz rozumieć jak działają modele AI — przyjdzie to z czasem. Chodzi tylko o to, żebyś wiedział, że matematyka którą właśnie poznałeś ma bezpośrednie zastosowanie.

---

### Czym jest "model AI"?

Zanim zaczniemy — wyjaśnijmy jedno słowo, które pojawia się w kontekście AI. **Model** to po prostu program komputerowy, który na podstawie danych wejściowych (np. zdjęcia, tekstu) produkuje jakiś wynik (np. "to jest kot", "to zdanie jest po angielsku").

I tutaj jest clue: **każdy model AI to, na najniższym poziomie, pewna funkcja matematyczna**.

### Model = funkcja

Wyobraź sobie, że budujesz program, który na podstawie powierzchni mieszkania przewiduje jego cenę.

```
wejście: powierzchnia (np. 55 m²)
wyjście: cena (np. 425 000 zł)

To jest FUNKCJA:
  f(55) = 425000
  f(80) = 600000
  ...
```

Najbardziej podstawowy model to właśnie prosta funkcja liniowa:

```
cena = a × powierzchnia + b

czyli: f(powierzchnia) = a × powierzchnia + b
```

To dokładnie ta sama funkcja `f(x) = ax + b`, którą poznałeś powyżej.

### Bardziej złożone modele

W zaawansowanych programach AI (takich jak ChatGPT czy modele rozpoznające zdjęcia) model to bardzo skomplikowane złożenie wielu funkcji. Pamiętasz złożenie funkcji `f(g(x))`? Tam takich złożeń mogą być miliony.

Kilka funkcji, które zobaczysz w AI kiedy będziesz dalej w kursie:

```
Sigmoid: σ(x) = 1 / (1 + e^(-x))
  → Zamienia dowolną liczbę na wartość między 0 a 1
  → Używana gdy model ma odpowiedzieć: "jak bardzo prawdopodobne?"

ReLU: f(x) = max(0, x)
  → Zwraca x jeśli x > 0, w przeciwnym razie zwraca 0
  → To nic innego niż bardzo prosta funkcja matematyczna
```

Nie przejmuj się na razie co to konkretnie robi w AI — ważne że widzisz: **to są zwykłe funkcje matematyczne**, dokładnie jak te, które właśnie poznałeś.

---

## 📌 Podsumowanie

| Pojęcie | Co to? |
|---------|--------|
| **Funkcja** | Reguła: jedno wejście → dokładnie jedno wyjście |
| **Dziedzina** | Zbiór dozwolonych wartości wejściowych |
| **Zbiór wartości** | Wszystkie możliwe wartości wyjściowe |
| **Funkcja liniowa** | `f(x) = ax + b` — wykres to prosta linia |
| **Funkcja kwadratowa** | `f(x) = ax² + bx + c` — wykres to parabola |
| **Złożenie funkcji** | Wynik jednej funkcji jako wejście do drugiej: `f(g(x))` |
